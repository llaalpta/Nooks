 <svg viewBox="0 0 170 52" xmlns="http://www.w3.org/2000/svg">
  <!-- Letra N -->
  <text x="0" y="48" font-family="Arial, sans-serif" font-size="60" font-weight="bold" fill="
#e1e2e9">n</text>

  <!-- Pin como letra O -->
  <g transform="translate(53, 30) scale(0.45)">
    <defs>
      <!-- Máscara para crear el agujero -->
      <mask id="pinMaskDark">
        <rect x="-50" y="-70" width="100" height="120" fill="white"/>
        <circle cx="0" cy="-25" r="25" fill="black"/>
      </mask>
    </defs>

    <!-- Cuerpo principal del pin con agujero -->
    <path d="M 0 40 Q -35 5 -35 -25 Q -35 -60 0 -60 Q 35 -60 35 -25 Q 35 5 0 40 Z" 
          fill="
#6366f1" mask="url(#pinMaskDark)"/>

    <!-- Texto "nook" sobre el agujero transparente -->
    <text x="" y="-20" text-anchor="middle" font-family="Arial, sans-serif" font-size="16" font-weight="bold" fill="
#e1e2e9">
      nook
    </text>
  </g>

  <!-- Letras O, K, S -->
  <text x="70" y="48" font-family="Arial, sans-serif" font-size="60" font-weight="bold" fill="
#e1e2e9">oks</text>
</svg>